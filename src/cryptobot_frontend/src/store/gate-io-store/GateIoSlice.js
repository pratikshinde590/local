import { createAsyncThunk, createSlice } from "@reduxjs/toolkit";
import AxiosInstance from "../../utils/AxiosInstance"


const GateIoSymbolSlice = createSlice({
    name: "gate-io",
    initialState: {},
    reducers: {},

    extraReducers: (builder) => {

    }
})

export default GateIoSymbolSlice.reducer